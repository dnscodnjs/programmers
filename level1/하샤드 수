class Solution {
    public boolean solution(int x) {
        
        int originX = x;
        int num =0;
        
        while(x>0){
            num += x%10;
            num /= 10;
        }
        
        return originX % num == 0;
    }
}
